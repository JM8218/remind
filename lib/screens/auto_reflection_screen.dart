import 'package:flutter/material.dart';

class AutoReflectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('자동 회고')),
      body: Center(child: Text('자동 회고 화면')),
    );
  }
}
