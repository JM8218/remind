import 'package:flutter/material.dart';

class EmotionAnalysisScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('감정 분석')),
      body: Center(child: Text('감정 분석 화면')),
    );
  }
}
